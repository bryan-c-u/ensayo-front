spring:
  application:
    name: ink-ms-users-dev

  datasource:
    url: jdbc:mysql://localhost:3306/user_ms_dev?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: bryan06uran
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format-sql: true
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 3002

jwt:
  secret: inklusport2024superSecretKeyForJWTtokenGenerationWith512bitsAlgorithmHS512
  expiration: 86400000

auth:
  service:
    url: http://localhost:3001

feign:
  circuitbreaker:
    enabled: true

logging:
  level:
    com.inklusport.users: DEBUG
